import { UI_ACTIONS } from '../action-types/action-types';

export const openModal = () => ({ type: UI_ACTIONS.UI_OPEN_MODAL });
export const closeModal = () => ({ type: UI_ACTIONS.UI_CLOSE_MODAL });
